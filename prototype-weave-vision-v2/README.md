# V2 — Capability Control Center prototype (duplicate of V1)

Working copy duplicated from V1 for the next iteration. V1 remains frozen under `../prototype-weave-vision/`.

- **Dev:** `npm install && npm run dev` → http://127.0.0.1:5175/
- **Build/publish:** `npm run build` → `prototype-capability-control-center-v2.html` + `assets/weave-vision-v2-app/`
